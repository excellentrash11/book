[build]
builder = "nixpacks"
buildCommand = "cd news-aggregator && pip install -r requirements.txt"

[deploy]
startCommand = "cd news-aggregator && gunicorn --bind 0.0.0.0:$PORT --timeout 120 app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
